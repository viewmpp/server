{{define "base"}}<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="description" content="Откройте файл MS Project (.mpp) в браузере и увидите диаграмму Ганта сразу — без установки и регистрации.">
<title>{{template "title" .}}</title>
<link rel="stylesheet" href="/static/css/app.css?v={{.Version}}">
{{block "head" .}}{{end}}
</head>
<body>
{{template "main" .}}
{{block "scripts" .}}{{end}}
</body>
</html>
{{end}}
