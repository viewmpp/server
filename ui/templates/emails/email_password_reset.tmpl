{{define "html"}}
    <h2>{{.Subject}}</h2>
    <p>{{.Message}}</p>
    <p>
        <a href="{{.Link}}"
           style="display:inline-block;padding:10px 16px;background:#3369d6;border-radius:4px;color:#fff;text-decoration:none;">
            Choose a new password
        </a>
    </p>
    <p style="color: #666; font-size: 14px;">{{.Hint}}</p>
{{end}}
